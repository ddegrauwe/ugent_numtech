This example runs the advection equation with a spike initial condition.

Compile with

	gfortran -O2 -fdefault-real-8 -o adveq adveq.F90 ~ddgrauwe/numtech/fft/lib/libfft.a

Run with
	
	./adveq

Use the ../show.R script to review results in R or RStudio with

	source('../show.R')
	animateResults('output.dat')
